// Общая база для полей ввода: цвета из токенов и охряное кольцо фокуса — то же,
// что у кнопок (см. button.tsx). Без явного стиля браузер рисует системное кольцо:
// на iOS оно голубое и к теме отношения не имеет.
//
// Скругление у всех полей одно и живёт здесь, а не на месте использования:
// input, textarea и select читаются как один элемент формы независимо от
// высоты и экрана. Размеры и отступы остаются у потребителя — они разные
// у формы входа, кабинета и фильтров.
// Кольцо внутреннее (ring-inset), а не наружное с отступом, как у кнопок:
// поля тянутся на всю ширину, и внешнее кольцо обрезалось краем модалки —
// ModalContent прокручивается, а значит и обрезает по обеим осям.
// Заливка холстом, а не --color-muted: поле обязано читаться «колодцем» на
// любой поверхности, а muted — накладка ПОВЕРХ неё. На карточке залитое muted
// поле выглядело бы приподнятым, тогда как поле по смыслу утоплено.
const FIELD_BASE = "rounded-lg border border-border bg-background text-foreground";

export const field =
  `${FIELD_BASE} focus-visible:[outline:none] focus-visible:border-ring ` +
  "focus-visible:ring-1 focus-visible:ring-ring focus-visible:ring-inset";

// Тот же вид для обёртки, внутри которой лежит настоящий input — поиск в шапке
// это form с прозрачным полем. :focus-visible на обёртке не сработает (фокус
// достаётся вложенному элементу), поэтому кольцо ловим через :focus-within.
export const fieldWithin =
  `${FIELD_BASE} focus-within:border-ring focus-within:ring-1 ` +
  "focus-within:ring-ring focus-within:ring-inset";

// Единственное поле мимо контракта — поиск в герое главной: он лежит НА
// фотографии, белым по затемнению. Токены интерфейса там означали бы не то,
// та же семья исключений, что кнопки поверх снимков.
//
// Соблюдение проверяется тестом tests/theme/fields.test.ts: собранное вручную
// поле отличается заливкой и кольцом фокуса, а ни tsc, ни check-theme этого
// не видят. Так поиск в чате однажды оказался на bg-muted.
