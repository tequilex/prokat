// Рамка карточки объявления: подложка, скошенные углы, обрезка содержимого.
// Живёт отдельной строкой классов по образцу ui/field.ts — её делят карточка
// выдачи и карточка кабинета, и без общего источника они разъедутся при первой
// же правке радиусов.
//
// Это НЕ общая система «скошенного угла»: рядом в проекте живут другие числа —
// 20/6 в блоке «Как это работает», 56px в галерее позиции. Сводить их к одной
// шкале — отдельная задача; здесь ровно рамка карточки объявления.
//
// Четыре длинные записи (border-top-left-radius и соседи) намеренно ложатся
// поверх короткой border-radius из .surface: скошенные углы — подпись выдачи, и
// перебить компонентный слой они обязаны. overflow-hidden обрезает по ним
// фотографию — она верх карточки и повторяет её форму.
export const cardFrame =
  "surface overflow-hidden rounded-tl-[26px] rounded-tr-[8px] " +
  "rounded-bl-[8px] rounded-br-[26px]";
